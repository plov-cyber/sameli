app_name: DummyModel Demo
metrics_port: 5000
enable_kafka: true

model_type: DummyModel
model_conf:
  name: dummy_v1

http_conf:
  host: 127.0.0.1
  port: 8000
  loop: uvloop

kafka_conf:
  consume_topic: test-topic
  produce_topic: test-topic-out

  consumer_conf:
    bootstrap_servers: localhost:9092
    group_id: sameli
    auto_offset_reset: latest
  producer_conf:
    bootstrap_servers: localhost:9092
